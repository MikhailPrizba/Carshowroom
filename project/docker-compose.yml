version: "3.9"

services:
  postgres:
    image: postgres:15.1-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: project

  web:
    build: .
    depends_on:
      - postgres
    ports:
      - "8000:8000"

volumes:
  - ./project:/project